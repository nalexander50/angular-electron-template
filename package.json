{
    "name": "atom",
    "version": "1.0.0",
    "main": "dist/app.js",
    "scripts": {
        "postinstall": "electron-builder install-app-deps",
        "start": "npm run build && electron .",
        "build": "tsc -p tsconfig.json && rsync -a ./web ./dist",
        "asar": "asar",
        "release": "npm run build && electron-builder",
        "prebuild": "rm -rf dist/ && bash -xe copy-ng.sh"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^17.0.19",
        "@typescript-eslint/eslint-plugin": "^5.12.1",
        "@typescript-eslint/parser": "^5.12.1",
        "asar": "^3.1.0",
        "electron": "^17.0.1",
        "electron-builder": "^22.14.13",
        "electron-reload": "^2.0.0-alpha.1",
        "eslint": "^8.9.0",
        "typescript": "^4.5.5",
        "wait-on": "^6.0.1"
    }
}
